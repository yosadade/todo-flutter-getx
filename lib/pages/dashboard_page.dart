import 'package:flutter/material.dart';

class DashbboardPage extends StatelessWidget {
  const DashbboardPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
